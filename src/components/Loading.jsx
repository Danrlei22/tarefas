import styles from './Loading.module.css';

const Loading = () => {
    return (
        <div className={styles.msgList}>
            <p>Carregando...</p>
        </div>
    );
}

export default Loading;